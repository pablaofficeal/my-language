import start  // Это будет начинать класс программы

Class   // Это будет начинать класс программы

{}   // Это будет начинать открытие класса 

print  // Это будет функция print для вывода на экран

give  // Это будет цикл для старта программы

()  // Это обявление конструкции

::  // Это оператор

function(main)  // Это функция

#build <clasic.pgt>  // Это подключение библиотеки

pint = test ("33")  // Это присвоение переменной и вызов функции

li = [1, 2, 3, 4, 5]  // Это создание списка

;  // Это сообщает о конце строки

vg // Это цикл

trs // Это импортируем функцию с стнадартной библиотеки Lib.pgt это пример

exit::();  // Это завершение Class

Method // Это метод класса

connect // Это подключение к чему либо

import exit::end::();  // Это завершение программы

// класичиский запуск программы демонстрация кода

#build <lib.pgt>
#build <lib.pgt, function>

import start

Class start {
    give()::{
        trs::(function);
        print::("function");
    }
    exit::();
}

import exit::end::();

// класичиский запуск программы демонстрация кода без использования библиотеки

import start 

Class main {
    give()::{
        print::("Hello world");
    }
    
    exit::();
}

import exit::end::();